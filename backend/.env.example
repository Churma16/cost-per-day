# Backend Environment Configuration Example
HOST=127.0.0.1
PORT=8080
GIN_MODE=debug
ALLOWED_ORIGINS=http://localhost:3000
DATABASE_PATH=./data/cost-per-day.db

# Google-only OIDC authentication.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SESSION_SECRET=replace-with-at-least-32-random-characters
APP_BASE_URL=http://localhost:3000
GOOGLE_REDIRECT_URI=http://localhost:8080/auth/google/callback

# Development-only authentication bypass. When enabled, requests use an explicit local
# development user instead of a production or historical owner identity.
AUTH_DISABLED=false

# Optional. Set this only when the backend should serve a compiled frontend.
# The production container sets STATIC_DIR=/app/web.
STATIC_DIR=
